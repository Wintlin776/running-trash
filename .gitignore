# Build artifacts (regenerated by ./build.sh)
build/

# macOS cruft
.DS_Store
*.swp

# The distributable DMG is intentionally tracked under docs/downloads/
# so GitHub Pages can serve it. Do not ignore it.
